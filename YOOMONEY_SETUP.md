# 💳 YooMoney Payment Integration Guide

## 📦 Что добавлено

Полная интеграция платёжной системы YooMoney с балансом пользователей.

---

## 🚀 Установка

### 1. Обновите код на сервере:

```bash
cd ~/merix_CodeX
git reset --hard origin/main
git pull origin main
```

### 2. Установите новую библиотеку:

```bash
pip3 install yoomoney==0.1.0
```

Или через requirements.txt:

```bash
pip3 install -r requirements.txt
```

### 3. Перезапустите бота:

```bash
pkill -f main.py
nohup python3 main.py > bot.log 2>&1 &
```

---

## ⚙️ Конфигурация

В файле `config.py` уже добавлены:

```python
YOOMONEY_TOKEN = "4100119383236022."
YOOMONEY_WALLET = "+992947764004"
```

**Важно:** Если токен истечёт, получите новый на https://yoomoney.ru/myservices/api.xml

---

## 🎯 Функциональность

### **1. Баланс пользователя**
- Автоматически добавляется колонка `balance` в таблицу `users`
- Новые пользователи регистрируются с балансом 0 RUB
- Баланс отображается в профиле: `💰 Баланс: {balance} RUB`

### **2. Пополнение баланса**
В профиле пользователя добавлена кнопка **"💳 Пополнить баланс"**

**Процесс пополнения:**
1. Пользователь нажимает кнопку
2. Вводит сумму (минимум 10 RUB)
3. Получает ссылку на оплату YooMoney
4. Оплачивает через банковскую карту
5. Нажимает "✅ Я оплатил"
6. Баланс автоматически начисляется после проверки

### **3. Проверка платежей**
- Используется YooMoney API для проверки статуса
- Уникальный `label` для каждого платежа: `user_{id}_{uuid}`
- Автоматическая верификация через `operation_history`

---

## 📊 База данных

### Обновления:
```sql
-- Автоматически добавляется при запуске бота
ALTER TABLE users ADD COLUMN balance INTEGER DEFAULT 0;
```

### Новые функции в database.py:
- `get_user_balance(user_id)` - получить баланс
- `update_user_balance(user_id, amount)` - добавить/вычесть сумму
- `set_user_balance(user_id, balance)` - установить баланс

---

## 🔐 Безопасность

✅ **Реализовано:**
- Уникальный label для каждого платежа
- Проверка минимальной суммы (10 RUB)
- Валидация через YooMoney API
- Логирование всех операций

⚠️ **Рекомендации:**
- Храните `YOOMONEY_TOKEN` в безопасности
- Регулярно проверяйте логи на подозрительные операции
- Используйте HTTPS для всех запросов

---

## 📝 Логи

Все операции записываются:
```
💳 Создана ссылка на оплату для пользователя 123: 100 RUB
✅ Платёж подтверждён: user=123, amount=100 RUB, label=user_123_abc123
💰 Баланс пользователя 123 изменён на 100 RUB
```

Просмотр логов:
```bash
tail -f bot.log
```

---

## 🛠️ Troubleshooting

### Ошибка: "ModuleNotFoundError: No module named 'yoomoney'"
```bash
pip3 install yoomoney
```

### Ошибка: "Invalid token"
- Проверьте токен в `config.py`
- Убедитесь что токен не истёк
- Получите новый токен на https://yoomoney.ru

### Баланс не начисляется
- Проверьте логи: `tail -f bot.log`
- Убедитесь что платёж имеет статус `success`
- Проверьте что label совпадает

---

## 🎨 Кастомизация

### Изменить минимальную сумму:
В файле `handlers/payment.py`, строка 44:
```python
if amount < 10:  # Измените 10 на нужное значение
```

### Добавить комиссию:
```python
amount_with_fee = int(amount * 1.05)  # +5% комиссия
```

---

## 📞 Поддержка

Вопросы по интеграции: @noxsec

---

## ✅ Checklist установки

- [ ] Обновил код: `git pull`
- [ ] Установил библиотеку: `pip3 install yoomoney`
- [ ] Проверил config.py (токен и кошелёк)
- [ ] Перезапустил бота
- [ ] Проверил работу в профиле
- [ ] Протестировал тестовый платёж

**Готово! Система платежей работает.** 🎉
