# 🚀 Деплой Merix Academy на PythonAnywhere

## 📝 Инструкция по обновлению кода на сервере

### Шаг 1: Подключитесь к Bash консоли PythonAnywhere

Перейдите на https://www.pythonanywhere.com → `Consoles` → откройте Bash консоль

---

### Шаг 2: Обновите код из GitHub

```bash
# Перейдите в директорию бота
cd ~/merix_CodeX

# Остановите бота (если запущен)
pkill -f main.py

# Получите последние изменения из GitHub
git pull origin main

# Или если нужно полностью перезагрузить (с потерей локальных изменений):
# git fetch --all
# git reset --hard origin/main
```

---

### Шаг 3: Проверьте обновления

```bash
# Проверьте, что новые файлы появились
ls -la handlers/merix_academy.py
ls -la MERIX_ACADEMY_README.md

# Проверьте изменения в config.py
tail -n 10 config.py
```

Вы должны увидеть:
```python
# ID приватного канала для платных подписок (Merix Academy)
PRIVATE_CHANNEL_ID = -1003543534808

# Реквизиты для оплаты картой
PAYMENT_CARD_ALIF = "+992888788181"
PAYMENT_CARD_MASTERCARD = "5413525250170749"
```

---

### Шаг 4: Проверьте зависимости (если нужно)

```bash
# Активируйте виртуальное окружение (если используется)
source venv/bin/activate

# Проверьте установленные пакеты
pip list | grep aiogram
```

---

### Шаг 5: Протестируйте бота

```bash
# Запустите тест
python3 test_academy.py
```

Ожидаемый результат:
```
✅ PASSED - Импорты модулей
✅ PASSED - Файловая структура
✅ PASSED - Конфигурация
```

---

## 🔄 Настройка Always-On бота (24/7)

### Вариант 1: Использование Always-On Task (Платная подписка)

Если у вас **платная** подписка PythonAnywhere:

1. Перейдите в **Tasks** → **Scheduled tasks**
2. Создайте новую задачу:
   - **Command:** `cd /home/ВАШ_USERNAME/merix_CodeX && python3 main.py`
   - **Hour:** `00:00` (полночь)
   - **Description:** Merix CodeX Bot Auto-Restart

3. Или используйте Always-On процесс:
   - Перейдите в **Web** → **Always-on tasks**
   - Создайте задачу с командой: `/home/ВАШ_USERNAME/merix_CodeX/main.py`

---

### Вариант 2: Использование `nohup` (Бесплатная подписка)

```bash
# Запустите бота в фоновом режиме
cd ~/merix_CodeX
nohup python3 main.py > bot.log 2>&1 &

# Проверьте, что бот запущен
ps aux | grep main.py

# Проверьте логи
tail -f bot.log
```

**Остановить бота:**
```bash
pkill -f main.py
```

**Перезапустить бота:**
```bash
pkill -f main.py
sleep 2
cd ~/merix_CodeX
nohup python3 main.py > bot.log 2>&1 &
```

---

### Вариант 3: Создание скрипта автозапуска

Создайте файл `start_bot.sh`:

```bash
cd ~/merix_CodeX
cat > start_bot.sh << 'EOF'
#!/bin/bash

# Остановить старый процесс
pkill -f main.py

# Подождать немного
sleep 2

# Перейти в директорию бота
cd ~/merix_CodeX

# Запустить бота
nohup python3 main.py > bot.log 2>&1 &

echo "✅ Бот запущен!"
echo "📊 Проверить статус: ps aux | grep main.py"
echo "📝 Посмотреть логи: tail -f ~/merix_CodeX/bot.log"
EOF

# Сделайте скрипт исполняемым
chmod +x start_bot.sh

# Запустите бота
./start_bot.sh
```

---

### Вариант 4: Настройка через cron (Автоматический перезапуск)

```bash
# Откройте crontab
crontab -e

# Добавьте эту строку (перезапуск каждые 4 часа):
0 */4 * * * cd /home/ВАШ_USERNAME/merix_CodeX && /usr/bin/python3 main.py >> bot.log 2>&1

# Или для запуска при старте системы:
@reboot cd /home/ВАШ_USERNAME/merix_CodeX && /usr/bin/python3 main.py >> bot.log 2>&1

# Сохраните и выйдите (Ctrl+X, затем Y, затем Enter)
```

---

## 📊 Мониторинг бота

### Проверка статуса:
```bash
# Проверить, запущен ли бот
ps aux | grep main.py

# Посмотреть последние логи
tail -n 50 ~/merix_CodeX/bot.log

# Следить за логами в реальном времени
tail -f ~/merix_CodeX/bot.log

# Проверить использование памяти
ps aux | grep python3 | grep main.py
```

### Ротация логов:
```bash
# Очистить большой лог-файл
echo "" > ~/merix_CodeX/bot.log

# Или создать резервную копию
cp ~/merix_CodeX/bot.log ~/merix_CodeX/bot_backup_$(date +%Y%m%d).log
echo "" > ~/merix_CodeX/bot.log
```

---

## 🔍 Проверка работы Merix Academy

После запуска бота проверьте:

1. **Откройте бота в Telegram:**
   - /start → 📂 Услуги → 🎓 Merix Academy

2. **Проверьте, что отображается:**
   - ✅ Фото с описанием
   - ✅ 4 кнопки с тарифами
   - ✅ Кнопка "💳 Оплата Картой"

3. **Проверьте в логах:**
```bash
grep "merix_academy" ~/merix_CodeX/bot.log
grep "Merix Academy" ~/merix_CodeX/bot.log
```

---

## ⚠️ Важные замечания для PythonAnywhere

### 1. Прокси настройки
Убедитесь, что в `config.py` прокси настроен (если нужно):
```python
PROXY_URL = os.getenv("PROXY_URL", None)
```

### 2. Ограничения бесплатного аккаунта:
- ❌ Нет Always-On процессов
- ❌ Нет входящих соединений
- ✅ Polling режим работает
- ✅ Можно использовать `nohup` + `cron`

### 3. Telegram Bot API доступен:
PythonAnywhere **разрешает** подключение к:
- `api.telegram.org` ✅
- `core.telegram.org` ✅

---

## 📝 Полный скрипт деплоя (копипаста)

```bash
# ========================================
# ПОЛНЫЙ СКРИПТ ОБНОВЛЕНИЯ И ЗАПУСКА БОТА
# ========================================

# 1. Остановить бота
pkill -f main.py

# 2. Перейти в директорию
cd ~/merix_CodeX

# 3. Получить обновления
git pull origin main

# 4. Проверить новые файлы
ls -la handlers/merix_academy.py

# 5. Запустить тесты
python3 test_academy.py

# 6. Запустить бота в фоне
nohup python3 main.py > bot.log 2>&1 &

# 7. Проверить запуск
sleep 3
ps aux | grep main.py

# 8. Посмотреть логи
tail -n 20 bot.log

echo "✅ Деплой завершен!"
echo "📊 Проверьте бота в Telegram: /start → Services → Merix Academy"
```

---

## 🛠 Решение проблем

### Проблема 1: Бот не запускается
```bash
# Проверьте ошибки
cat bot.log | tail -n 50

# Проверьте Python
which python3
python3 --version

# Проверьте зависимости
pip list | grep aiogram
```

### Проблема 2: Git pull выдает ошибку
```bash
# Сбросить локальные изменения
git fetch --all
git reset --hard origin/main
```

### Проблема 3: Бот работает, но Merix Academy не появляется
```bash
# Проверьте, что файлы обновлены
grep "merix_academy" handlers/__init__.py
grep "PRIVATE_CHANNEL_ID" config.py

# Перезапустите бота
pkill -f main.py
sleep 2
nohup python3 main.py > bot.log 2>&1 &
```

---

## 🎉 Готово!

**Бот обновлен и запущен на PythonAnywhere!**

### Следующие шаги:
1. ✅ Откройте бота в Telegram
2. ✅ Проверьте работу Merix Academy
3. ✅ Добавьте бота в канал -1003543534808 как админа
4. ✅ Протестируйте оплату через Stars и карту

---

**Поддержка:** @noxsec  
**Дата:** 1 января 2026 г.  
**Версия:** Production Ready
