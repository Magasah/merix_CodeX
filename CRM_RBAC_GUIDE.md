# 🛡️ CRM System & RBAC Security Implementation

## 🎯 Что добавлено

Полная CRM система с ролевым контролем доступа (RBAC) и защитой от подделки inline-кнопок.

---

## 🔐 RBAC: Role-Based Access Control

### **Иерархия ролей:**
```
user < manager < admin
```

- **user** (по умолчанию) - обычный клиент
- **manager** - менеджер с доступом к CRM
- **admin** - администратор с полным доступом

### **Особенности:**
- `ADMIN_ID` (из config.py) **ВСЕГДА** имеет роль admin (hardcoded)
- Автоматическая миграция БД - добавляется колонка `role TEXT DEFAULT 'user'`
- Проверка прав через функцию `has_permission(user_id, required_role)`

---

## 🛡️ Защита от атак

### **Проблема:**
Пользователь может переслать сообщение с inline-кнопкой "Админ-панель" и нажать её, пытаясь получить доступ.

### **Решение:**
Каждый callback-обработчик проверяет:
```python
if not db.has_permission(user_id, 'admin'):
    await callback.answer("⛔ Доступ запрещён", show_alert=True)
    logger.warning(f"⚠️ Попытка неавторизованного доступа: user_id={user_id}")
    return
```

### **Защищённые обработчики:**
- ✅ `admin_stats` - только admin
- ✅ `admin_orders` - только admin
- ✅ `order_view_*` - только admin
- ✅ `order_status_*` - только admin
- ✅ `admin_broadcast` - только admin
- ✅ `admin_reply_id` - manager+
- ✅ `create_invoice` - manager+

---

## 👨‍💼 Управление ролями (Admin Only)

### **Назначить менеджера:**
```
/set_manager {user_id}
```
Пример: `/set_manager 123456789`

**Что происходит:**
1. Проверяется что команду вызвал ADMIN_ID
2. Устанавливается роль 'manager' в БД
3. Пользователю отправляется уведомление
4. Логируется действие

---

### **Снять менеджера:**
```
/fire_manager {user_id}
```
Пример: `/fire_manager 123456789`

**Что происходит:**
1. Роль возвращается к 'user'
2. Пользователю отправляется уведомление
3. Логируется действие

---

### **Список персонала:**
```
/list_staff
```

Показывает всех admin и manager с их данными:
- ID, Имя, Username, Роль

---

## 👨‍💻 Панель менеджера

### **Команда:**
```
/staff
```

**Доступ:** manager + admin

**Функции:**
- ✉️ **Ответить по ID** - отправить сообщение пользователю
- 🧾 **Выставить счёт** - создать инвойс для клиента

**Что НЕ видят менеджеры:**
- ❌ Статистику бота
- ❌ Панель рассылки
- ❌ Управление заказами
- ❌ Сырые данные БД

---

## 🧾 Система выставления счетов

### **Процесс (FSM):**

1. **Manager/Admin нажимает "🧾 Выставить счёт"**
   - Бот проверяет права доступа
   - Запрашивает ID пользователя

2. **Ввод ID клиента:**
   ```
   Введите ID: 123456789
   ```
   - Проверяется существование в БД

3. **Ввод суммы:**
   ```
   Введите сумму: 500
   ```
   - Минимум 10 RUB

4. **Ввод описания:**
   ```
   Введите описание: Создание сайта
   ```

5. **Создание счёта:**
   - Генерируется уникальный `label: invoice_{user_id}_{uuid}`
   - Создаётся ссылка YooMoney Quickpay
   - Клиенту отправляется сообщение:

   ```
   🧾 Вам выставлен счёт!
   
   📝 Услуга: Создание сайта
   💰 Сумма: 500 RUB
   🆔 ID счёта: invoice_123_abc123
   
   [💳 Оплатить сейчас] [✅ Я оплатил]
   ```

6. **Проверка оплаты:**
   - Клиент нажимает "✅ Я оплатил"
   - Бот проверяет через YooMoney API
   - При успехе: баланс пополняется автоматически

---

## 📊 База данных

### **Новая колонка:**
```sql
ALTER TABLE users ADD COLUMN role TEXT DEFAULT 'user';
```

### **Функции:**
- `get_user_role(user_id)` - получить роль
- `set_user_role(user_id, role)` - установить роль
- `has_permission(user_id, required_role)` - проверить доступ

---

## 🚀 Установка на сервере

```bash
cd ~/merix_CodeX
git reset --hard origin/main
git pull origin main
pkill -f main.py
nohup python3 main.py > bot.log 2>&1 &
```

---

## 🔍 Логи безопасности

Все попытки несанкционированного доступа логируются:

```
⚠️ Попытка неавторизованного доступа к статистике: user_id=123456
⚠️ Попытка использования /set_manager не-админом: user_id=789
⚠️ Попытка неавторизованного доступа к созданию счёта: user_id=456
```

Просмотр логов:
```bash
tail -f bot.log | grep "⚠️"
```

---

## 🎯 Примеры использования

### **Сценарий 1: Назначение менеджера**
1. Admin: `/set_manager 123456789`
2. Бот: "✅ Роль назначена!"
3. Пользователю 123456789 приходит уведомление
4. Теперь он может использовать `/staff`

### **Сценарий 2: Выставление счёта**
1. Manager: `/staff` → нажимает "🧾 Выставить счёт"
2. Вводит ID клиента: `987654321`
3. Вводит сумму: `1000`
4. Вводит описание: `Разработка Telegram бота`
5. Клиенту 987654321 приходит счёт на оплату
6. Клиент оплачивает через YooMoney
7. Баланс автоматически пополняется

### **Сценарий 3: Попытка атаки**
1. Злоумышленник пересылает себе сообщение с кнопкой "Админ-панель"
2. Нажимает кнопку
3. Бот проверяет: `has_permission(user_id, 'admin')` → False
4. Ответ: "⛔ Доступ запрещён"
5. Логируется: "⚠️ Попытка неавторизованного доступа"

---

## 🛠️ Кастомизация

### **Добавить новую роль:**
Обновите иерархию в `database.py`:
```python
roles_hierarchy = {'user': 0, 'manager': 1, 'supervisor': 2, 'admin': 3}
```

### **Ограничить функцию для определённой роли:**
```python
if not db.has_permission(user_id, 'supervisor'):
    await callback.answer("⛔ Доступ запрещён", show_alert=True)
    return
```

---

## ✅ Checklist безопасности

- [x] Проверка прав в каждом callback
- [x] Логирование всех попыток доступа
- [x] Hardcoded admin через ADMIN_ID
- [x] Уникальные label для счетов
- [x] Проверка существования пользователя перед действиями
- [x] Миграция БД без потери данных
- [x] FSM с повторными проверками прав на каждом шаге

---

## 📞 Поддержка

Вопросы по CRM: @noxsec

---

## 🎉 Готово!

CRM система с RBAC полностью рабочая и защищённая от атак! 🛡️
