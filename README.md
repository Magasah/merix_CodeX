# 🤖 Merix CodeX Bot

Профессиональный Telegram бот для IT-агентства **Merix CodeX**, специализирующегося на разработке ботов, веб-сайтов и услугах информационной безопасности.

## 📋 Описание

Это полнофункциональный сервисный бот, который позволяет:
- 📂 Просматривать услуги компании с подробными описаниями
- 👤 Управлять профилем пользователя
- ✅ Оформлять заказы через систему FSM (Finite State Machine)
- 🔔 Автоматически отправлять заявки администратору
- 🔙 Удобную навигацию с кнопками "Назад"

## 🚀 Технологии

- **Python 3.8+**
- **aiogram 3.7.0** - современная асинхронная библиотека для Telegram Bot API
- **python-dotenv** - управление переменными окружения
- **FSM (Finite State Machine)** - управление состояниями диалога
- **Inline & Reply клавиатуры** - современный UI

## 📁 Структура проекта

```
telegram_bot/
├── main.py                 # Точка входа приложения
├── config.py              # Конфигурация и переменные окружения
├── requirements.txt       # Зависимости проекта
├── .env                   # Переменные окружения (создать вручную)
├── .env.example          # Пример файла .env
├── .gitignore            # Игнорируемые файлы
├── README.md             # Документация проекта
│
├── handlers/             # Обработчики команд и сообщений
│   ├── __init__.py
│   ├── start.py         # Команда /start
│   ├── services.py      # Раздел услуг
│   ├── profile.py       # Профиль пользователя
│   ├── about.py         # О компании
│   ├── help.py          # Помощь
│   └── order.py         # Система заказов с FSM
│
├── keyboards/            # Клавиатуры бота
│   ├── __init__.py
│   ├── reply.py         # Reply клавиатуры (постоянные)
│   └── inline.py        # Inline клавиатуры (под сообщениями)
│
└── states/               # FSM состояния
    ├── __init__.py
    └── order.py         # Состояния для процесса заказа
```

## ⚙️ Установка и настройка

### 1. Клонирование репозитория

```bash
cd telegram_bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
copy .env.example .env
```

Откройте `.env` и укажите свои данные:

```env
# Получите токен у @BotFather в Telegram
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Ваш Telegram ID (можно узнать у @userinfobot)
ADMIN_ID=123456789
```

### 4. Запуск бота

```bash
python main.py
```

После успешного запуска вы увидите:
```
🤖 Бот Merix CodeX запущен!
📊 Зарегистрировано роутеров: 6
👨‍💼 ID администратора: 123456789
```

## 🎯 Основные функции

### 1. Команда /start
Приветствует пользователя и показывает главное меню с 4 кнопками:
- 📂 Услуги
- 👤 Профиль
- ℹ️ О компании
- 🆘 Помощь

### 2. Раздел "Услуги"
- Показывает 3 категории: Telegram Боты, Веб-сайты, Безопасность
- Каждая категория содержит подробное описание, стоимость и сроки
- Кнопка "Заказать" запускает процесс оформления заявки
- Кнопка "Назад" возвращает к списку категорий

### 3. Система заказов (FSM)
**Процесс оформления заказа:**

1. Пользователь нажимает "✅ Заказать"
2. Бот просит описать задачу
3. Пользователь отправляет текстовое описание
4. Бот показывает превью заявки с кнопками [Отправить] / [Отменить]
5. При подтверждении:
   - Заявка отправляется администратору
   - Пользователь получает уведомление об успешной отправке

### 4. Профиль
Показывает информацию о пользователе:
- Имя
- Username
- Telegram ID

### 5. О компании
Информация о Merix CodeX, миссии и ценностях компании.

### 6. Помощь
Подробная инструкция по использованию бота и контакты поддержки.

## 🔧 Настройка и кастомизация

### Изменение текстов

Все тексты находятся в соответствующих обработчиках в папке `handlers/`.

**Пример:** Изменить приветственное сообщение в `handlers/start.py`:
```python
welcome_text = (
    f"👋 Ваш текст приветствия..."
)
```

### Добавление новых услуг

В файле `handlers/services.py` найдите словарь `SERVICES_INFO` и добавьте новую услугу:

```python
SERVICES_INFO = {
    "new_service": {
        "title": "🎨 Дизайн",
        "description": "Описание услуги..."
    }
}
```

### Изменение цен и сроков

Отредактируйте соответствующие строки в `SERVICES_INFO` в файле `handlers/services.py`.

## 📊 Логирование

Бот использует стандартный модуль `logging` для отслеживания событий:
- INFO - общая информация (запуск бота, регистрация роутеров)
- ERROR - ошибки выполнения
- WARNING - предупреждения

Логи выводятся в консоль в формате:
```
2025-12-28 10:30:45 - __main__ - INFO - 🤖 Бот Merix CodeX запущен!
```

## 🛡️ Безопасность

- **Токен бота** хранится в `.env` файле и не попадает в репозиторий
- `.env` добавлен в `.gitignore`
- Используйте `.env.example` как шаблон для других разработчиков

## 🚨 Решение проблем

### Ошибка: "BOT_TOKEN не найден"
**Решение:** Убедитесь, что файл `.env` существует и содержит переменную `BOT_TOKEN`.

### Ошибка: "ADMIN_ID должен быть числом"
**Решение:** В `.env` файле `ADMIN_ID` должен быть числовым значением без кавычек.

### Бот не отвечает на команды
**Решение:**
1. Проверьте, что бот запущен (`python main.py`)
2. Убедитесь, что токен правильный
3. Проверьте интернет-соединение

## 📝 Лицензия

Этот проект создан для образовательных целей и использования в рамках компании Merix CodeX.

## 👨‍💻 Автор

Разработано для IT-агентства **Merix CodeX**

---

**Версия:** 1.0.0  
**Дата:** Декабрь 2025  
**Python:** 3.8+  
**aiogram:** 3.7.0
