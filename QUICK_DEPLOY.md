# 📋 БЫСТРАЯ ИНСТРУКЦИЯ - Копируй и вставляй

## 🔥 ДЛЯ PYTHONANYWHERE - КОМАНДЫ ДЛЯ КОПИРОВАНИЯ

### 1️⃣ Обновить код из GitHub (вставьте всё сразу)

```bash
cd ~/merix_CodeX
pkill -f main.py
git pull origin main
python3 test_academy.py
```

---

### 2️⃣ Запустить бота 24/7 (выберите один вариант)

#### ✅ Вариант А: Простой запуск (скопируйте и вставьте)
```bash
cd ~/merix_CodeX
nohup python3 main.py > bot.log 2>&1 &
echo "✅ Бот запущен! Проверка: ps aux | grep main.py"
tail -f bot.log
```

#### ✅ Вариант Б: Создать скрипт автозапуска (скопируйте всё)
```bash
cd ~/merix_CodeX
cat > start_bot.sh << 'EOF'
#!/bin/bash
pkill -f main.py
sleep 2
cd ~/merix_CodeX
nohup python3 main.py > bot.log 2>&1 &
echo "✅ Бот запущен!"
ps aux | grep main.py
EOF
chmod +x start_bot.sh
./start_bot.sh
```

#### ✅ Вариант В: Автоперезапуск через cron (скопируйте всё)
```bash
(crontab -l 2>/dev/null; echo "0 */4 * * * cd /home/\$USER/merix_CodeX && /usr/bin/python3 main.py >> bot.log 2>&1") | crontab -
echo "✅ Cron настроен! Бот будет перезапускаться каждые 4 часа"
```

**⚠️ ВАЖНО:** Если видите ошибку `python3/home/...`, используйте эту команду:
```bash
(crontab -l 2>/dev/null; echo "0 */4 * * * cd ~/merix_CodeX && nohup python3 main.py >> bot.log 2>&1 &") | crontab -
```

---

### 3️⃣ Проверить статус бота (мониторинг)

```bash
# Проверить, запущен ли бот
ps aux | grep main.py

# Посмотреть последние 30 строк логов
tail -n 30 ~/merix_CodeX/bot.log

# Следить за логами в реальном времени (Ctrl+C для выхода)
tail -f ~/merix_CodeX/bot.log
```

---

### 4️⃣ Перезапустить бота

```bash
cd ~/merix_CodeX
pkill -f main.py
sleep 2
nohup python3 main.py > bot.log 2>&1 &
ps aux | grep main.py
```

---

### 5️⃣ Остановить бота

```bash
pkill -f main.py
echo "✅ Бот остановлен"
```

---

## 🎯 ПОЛНЫЙ СКРИПТ ДЕПЛОЯ (ВСЁ В ОДНОМ)

**Скопируйте всё и вставьте в PythonAnywhere Bash:**

```bash
#!/bin/bash
echo "🚀 Начинаем деплой Merix Academy..."

# 1. Остановить бота
echo "⏹️ Остановка бота..."
pkill -f main.py
sleep 2

# 2. Обновить код
echo "📥 Получение обновлений из GitHub..."
cd ~/merix_CodeX
git pull origin main

# 3. Проверить файлы
echo "🔍 Проверка новых файлов..."
if [ -f "handlers/merix_academy.py" ]; then
    echo "✅ merix_academy.py найден"
else
    echo "❌ merix_academy.py НЕ найден!"
fi

# 4. Запустить тесты
echo "🧪 Запуск тестов..."
python3 test_academy.py

# 5. Запустить бота
echo "▶️ Запуск бота..."
nohup python3 main.py > bot.log 2>&1 &
sleep 3

# 6. Проверить запуск
echo "📊 Проверка статуса..."
if ps aux | grep -q "[m]ain.py"; then
    echo "✅ Бот успешно запущен!"
    echo "📝 Процессы:"
    ps aux | grep "[m]ain.py"
else
    echo "❌ Бот НЕ запущен! Проверьте логи:"
    tail -n 20 bot.log
fi

# 7. Показать последние логи
echo ""
echo "📋 Последние логи бота:"
echo "========================"
tail -n 15 bot.log

echo ""
echo "🎉 Деплой завершен!"
echo "📱 Проверьте бота: /start → Services → Merix Academy"
echo "📊 Мониторинг: tail -f ~/merix_CodeX/bot.log"
```

---

## 💡 ЧАСТЫЕ КОМАНДЫ

### Посмотреть логи
```bash
tail -n 50 ~/merix_CodeX/bot.log
```

### Очистить логи
```bash
echo "" > ~/merix_CodeX/bot.log
```

### Проверить обновления Git
```bash
cd ~/merix_CodeX
git status
git log --oneline -5
```

### Принудительно обновить (сбросить локальные изменения)
```bash
cd ~/merix_CodeX
git fetch --all
git reset --hard origin/main
```

---

## 🔧 НАСТРОЙКА TASKS (для платной подписки)

Если у вас **платная** подписка PythonAnywhere:

### Способ 1: Через веб-интерфейс
1. Перейдите: **Tasks** → **Scheduled tasks**
2. Команда: `cd /home/ВАШ_USERNAME/merix_CodeX && python3 main.py`
3. Интервал: `00:00` (ежедневно в полночь)

### Способ 2: Always-On (если доступно)
1. Перейдите: **Web** → **Always-on tasks**
2. Command: `/home/ВАШ_USERNAME/merix_CodeX/main.py`
3. Working directory: `/home/ВАШ_USERNAME/merix_CodeX`

---

## 🎯 ЧЕКЛИСТ ПОСЛЕ ДЕПЛОЯ

- [ ] Код обновлен: `git pull origin main`
- [ ] Файл merix_academy.py существует
- [ ] Тесты пройдены: `python3 test_academy.py`
- [ ] Бот запущен: `ps aux | grep main.py`
- [ ] Логи без ошибок: `tail -n 20 bot.log`
- [ ] Бот отвечает в Telegram
- [ ] Кнопка "🎓 Merix Academy" видна
- [ ] Бот добавлен в канал -1003543534808 как админ

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Git pull выдает ошибку:
```bash
cd ~/merix_CodeX
git stash
git pull origin main
```

### Бот не запускается:
```bash
cd ~/merix_CodeX
python3 main.py
# Посмотрите ошибку, затем Ctrl+C
```

### Merix Academy не появляется:
```bash
cd ~/merix_CodeX
grep "merix_academy" handlers/__init__.py
grep "PRIVATE_CHANNEL_ID" config.py
```

### Нужна помощь:
```bash
# Скопируйте и отправьте логи
tail -n 50 ~/merix_CodeX/bot.log > errors.txt
cat errors.txt
```

---

## 📱 ПРОВЕРКА РАБОТЫ

После деплоя откройте бота в Telegram:

1. `/start`
2. `📂 Услуги`
3. `🎓 Merix Academy`

**Должно появиться:**
- ✅ Фото с описанием
- ✅ 4 кнопки с тарифами (7/14/30 дней + Карта)
- ✅ Кнопка "Назад"

---

## 🎉 ГОТОВО!

**Все команды скопированы и готовы к использованию!**

**Следующий шаг:** Откройте PythonAnywhere Bash консоль и вставьте нужные команды.

---

**Дата:** 1 января 2026 г.  
**Версия:** Quick Reference v1.0  
**Поддержка:** @noxsec
