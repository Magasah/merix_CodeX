# 🌐 Настройка прокси для Telegram бота

## Проблема
Если вы получаете ошибку:
```
ClientConnectorError: Cannot connect to host api.telegram.org:443
```

Это означает, что доступ к Telegram API заблокирован в вашем регионе.

## Решения

### ✅ Вариант 1: Использование бесплатного прокси

1. **Найдите рабочий SOCKS5 прокси:**
   - https://www.proxy-list.download/SOCKS5
   - https://spys.one/en/socks-proxy-list/
   - https://hidemy.name/ru/proxy-list/

2. **Добавьте прокси в `.env` файл:**
```env
PROXY_URL=socks5://прокси_адрес:порт
```

**Пример:**
```env
PROXY_URL=socks5://45.140.13.119:9132
```

**С авторизацией:**
```env
PROXY_URL=socks5://username:password@45.140.13.119:9132
```

3. **Перезапустите бота**

### ✅ Вариант 2: Использование VPN

Если у вас есть VPN:
1. Подключитесь к VPN
2. Оставьте `PROXY_URL` пустым в `.env`
3. Запустите бота

### ✅ Вариант 3: Платный прокси (рекомендуется для production)

**Хорошие сервисы:**
- **Proxy6.net** - от 50₽/месяц
- **ProxyLine.net** - от 100₽/месяц
- **MyProxy.online** - от 75₽/месяц

**Преимущества:**
- Стабильное соединение
- Высокая скорость
- Поддержка 24/7

### ✅ Вариант 4: Локальный Bot API сервер (advanced)

Для продвинутых пользователей можно развернуть собственный Bot API сервер на VPS за пределами блокировок.

## Тестирование прокси

Перед добавлением в бота, проверьте прокси:

```bash
curl --proxy socks5://адрес:порт https://api.telegram.org/bot<ВАШ_ТОКЕН>/getMe
```

Если получили JSON ответ - прокси работает!

## Примечания

⚠️ **Бесплатные прокси:**
- Могут быть нестабильными
- Медленная скорость
- Часто перестают работать
- Не рекомендуется для production

✅ **Платные прокси:**
- Стабильная работа
- Высокая скорость
- Техническая поддержка
- Рекомендуется для production

## Поддержка

Если проблемы остались, проверьте:
1. Правильность формата `PROXY_URL`
2. Работоспособность прокси
3. Наличие интернет-соединения
4. Корректность токена бота
