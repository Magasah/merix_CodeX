# 🚀 MERIX CODEX BOT - UPGRADE GUIDE v2.0

## 📋 Что добавлено в новой версии

### ✨ Новые функции

1. **🌐 Telegram Web App Integration**
   - Кнопка "Услуги" теперь открывает Web App: https://merix-codex.netlify.app
   - Использует WebAppInfo API

2. **💾 Обновленная база данных**
   - Добавлено поле `referrer_id` (INTEGER) - ID пригласившего пользователя
   - Добавлено поле `balance` (INTEGER, default 0) - бонусный баланс пользователя
   - Поддержка реферальных ссылок вида `t.me/bot?start=12345`

3. **👥 Реферальная система**
   - Каждый пользователь получает уникальную реферальную ссылку
   - В профиле отображается: "Invite a friend and get 10% discount!"
   - Автоматическое сохранение referrer_id при регистрации

4. **📦 Новые пакеты услуг**
   - **Fast Start Package** (от 700 TJS): Landing + Bot + Channel Design
   - **AI Automation** (от 400 TJS): Custom AI Assistant для бизнеса
   - **Tech Support 24/7** (150 TJS/месяц): Мониторинг и защита

5. **⭐ Система отзывов**
   - Кнопка "⭐ Reviews" в главном меню
   - [Read Reviews] - ссылка на канал
   - [Write Review] - пользователь пишет отзыв → бот пересылает админу для модерации

6. **📊 Расширенная админ-панель**
   - Команда `/admin` (только для ADMIN_ID)
   - [📊 Stats]: Всего пользователей + Новых сегодня
   - [📢 Broadcast]: Рассылка с защитой от flood limits (asyncio.sleep(0.05))
   - Поддержка текстовых сообщений и фото

7. **🔒 Безопасность и стабильность**
   - Готово для PythonAnywhere с proxy поддержкой
   - FSM для Broadcast и Reviews
   - Улучшенная обработка ошибок

---

## 🛠️ Инструкция по обновлению

### Шаг 1: Обновите базу данных

Бот автоматически обновит структуру таблицы при первом запуске, НО если у вас уже есть данные:

```bash
# Запустите Python и выполните миграцию:
python -c "import sqlite3; conn = sqlite3.connect('bot_database.db'); conn.execute('ALTER TABLE users ADD COLUMN referrer_id INTEGER'); conn.execute('ALTER TABLE users ADD COLUMN balance INTEGER DEFAULT 0'); conn.commit(); conn.close(); print('✅ Database upgraded!')"
```

### Шаг 2: Замените файлы

**Обязательные файлы для замены:**

1. `database.py` - Обновлен с новыми полями и функциями
2. `translations.py` - Добавлены новые тексты для всех 4 языков  
3. `keyboards/reply.py` - Web App button для "Услуги"
4. `keyboards/inline.py` - Новые кнопки сервисов и отзывов
5. `states/order.py` - Добавлены BroadcastStates и ReviewStates

**Новые файлы для добавления:**

6. `handlers/reviews.py` - Система отзывов (**НОВЫЙ**)

**Файлы для обновления (можно заменить на _UPGRADED версии):**

7. `main.py` → используйте `main_UPGRADED.py`
8. `handlers/start.py` → используйте `start_UPGRADED.py` (поддержка реферальных ссылок)
9. `handlers/profile.py` → используйте `profile_UPGRADED.py` (показывает реферальную ссылку)
10. `handlers/admin.py` → используйте `admin_UPGRADED.py` (расширенная статистика)

**Файлы, которые НЕ требуют изменений:**

- `config.py` ✅
- `middleware.py` ✅
- `handlers/services.py` ✅ (автоматически подхватит новые пакеты из translations.py)
- `handlers/about.py` ✅
- `handlers/help.py` ✅
- `handlers/order.py` ✅
- `handlers/subscription.py` ✅

### Шаг 3: Обновите handlers/__init__.py

```python
from .reviews import router as reviews_router

routers = [
    start_router,
    subscription_router,
    admin_router,
    services_router,
    profile_router,
    about_router,
    help_router,
    order_router,
    reviews_router  # ← ДОБАВЬТЕ ЭТО
]
```

### Шаг 4: Получите username вашего бота

```python
# Запустите в Python:
import asyncio
from aiogram import Bot

async def get_bot_username():
    bot = Bot(token="YOUR_BOT_TOKEN")
    me = await bot.get_me()
    print(f"Bot username: @{me.username}")
    await bot.session.close()

asyncio.run(get_bot_username())
```

Запомните username — он нужен для реферальных ссылок.

---

## 🌐 Настройка для PythonAnywhere

### Вариант 1: Через proxy (рекомендуется)

В `.env` добавьте:
```
PROXY_URL=http://proxy.server:3128
```

Или в `main_UPGRADED.py` раскомментируйте строку:
```python
session = AiohttpSession(proxy="http://proxy.server:3128")
```

### Вариант 2: Через cloudflare-warp (альтернатива)

```bash
pip install aiogram[fast]
# Используйте cloudflare warp для обхода блокировок
```

---

## 📝 Полный список изменений в коде

### database.py

**Добавлено:**
- `referrer_id INTEGER` в таблицу users
- `balance INTEGER DEFAULT 0` в таблицу users
- Функция `get_users_count_today()` - статистика за сегодня
- Функция `get_user_info(user_id)` - полная информация о пользователе
- Параметр `referrer_id` в функцию `add_user()`

### translations.py

**Добавлено для всех 4 языков:**
- `btn_reviews` - кнопка "⭐ Отзывы"
- `btn_fast_start` - "📦 Пакет Fast Start"
- `btn_ai_automation` - "🤖 AI Автоматизация"
- `btn_tech_support` - "🛡️ Тех. поддержка"
- `service_fast_start` - описание пакета
- `service_ai_automation` - описание AI
- `service_tech_support` - описание поддержки
- `profile_text` - обновлен с реферальной ссылкой и балансом
- `reviews_menu` - меню отзывов
- `btn_read_reviews`, `btn_write_review` - кнопки
- `review_prompt`, `review_sent`, `review_to_admin` - тексты системы отзывов
- `statistics_text` - обновлен с `{today}`

### keyboards/reply.py

**Изменено:**
```python
from aiogram.types import WebAppInfo  # ДОБАВЛЕНО

KeyboardButton(
    text=get_text(lang, 'btn_services'),
    web_app=WebAppInfo(url="https://merix-codex.netlify.app")  # ДОБАВЛЕНО
)

KeyboardButton(text=get_text(lang, 'btn_reviews'))  # ДОБАВЛЕНО
```

### keyboards/inline.py

**Добавлено:**
- 3 новые кнопки в `get_services_keyboard()`:
  - service_fast_start
  - service_ai_automation
  - service_tech_support
- Новая функция `get_reviews_keyboard()`

### states/order.py

**Добавлено:**
```python
class BroadcastStates(StatesGroup):
    waiting_for_message = State()

class ReviewStates(StatesGroup):
    waiting_for_review = State()
```

### handlers/start.py (→ start_UPGRADED.py)

**Добавлено:**
- Парсинг реферальных ссылок из `/start 12345`
- Сохранение `referrer_id` при регистрации нового пользователя
- Логирование реферальных переходов

### handlers/profile.py (→ profile_UPGRADED.py)

**Добавлено:**
- Получение `user_info` из БД
- Формирование реферальной ссылки `https://t.me/{bot_username}?start={user_id}`
- Отображение бонусного баланса
- Текст о реферальной программе

### handlers/reviews.py (НОВЫЙ ФАЙЛ)

**Функции:**
- `show_reviews_menu()` - показывает меню отзывов
- `start_write_review()` - начинает FSM для написания отзыва
- `process_review()` - отправляет отзыв админу на модерацию
- `back_to_main()` - возврат в главное меню

### handlers/admin.py (→ admin_UPGRADED.py)

**Обновлено:**
- `show_statistics()` - теперь показывает `total` + `today`
- `process_broadcast()` - добавлена задержка `asyncio.sleep(0.05)`
- Поддержка рассылки фото (не только текста)
- Статусное сообщение во время рассылки

---

## 🧪 Тестирование новых функций

### 1. Реферальная система

```
1. Откройте профиль (@username)
2. Скопируйте реферальную ссылку
3. Откройте в инкогнито/другом аккаунте
4. Проверьте, что в БД у нового пользователя сохранился referrer_id
```

**SQL для проверки:**
```sql
SELECT user_id, referrer_id FROM users WHERE referrer_id IS NOT NULL;
```

### 2. Web App кнопка

```
1. Нажмите кнопку "📂 Услуги"
2. Должен открыться Web App: https://merix-codex.netlify.app
```

### 3. Новые пакеты услуг

```
1. Нажмите "📂 Услуги" (или используйте inline меню)
2. Проверьте, что отображаются 6 услуг:
   - Telegram Боты
   - Веб-сайты
   - Безопасность
   - Пакет "Быстрый старт" ← НОВОЕ
   - AI Автоматизация ← НОВОЕ
   - Тех. поддержка 24/7 ← НОВОЕ
```

### 4. Система отзывов

```
1. Нажмите "⭐ Отзывы"
2. Выберите "✍️ Написать отзыв"
3. Напишите текст
4. Проверьте, что админ получил сообщение с отзывом
```

### 5. Расширенная админ-панель

```
1. Отправьте /admin (от ADMIN_ID)
2. Нажмите "📊 Статистика"
3. Должно показать: "Всего: X, Новых сегодня: Y"
4. Нажмите "📢 Рассылка"
5. Отправьте тестовое сообщение
6. Проверьте, что все пользователи получили (с задержкой 0.05 сек)
```

---

## 🚨 Возможные проблемы и решения

### Проблема 1: "Module 'reviews' not found"

**Решение:** Убедитесь, что файл `handlers/reviews.py` существует и импортирован в `handlers/__init__.py`

### Проблема 2: "Column referrer_id does not exist"

**Решение:** Выполните миграцию БД (см. Шаг 1)

### Проблема 3: Web App не открывается

**Решение:** Проверьте, что используете aiogram 3.x и URL корректный

### Проблема 4: Рассылка вызывает FloodWait

**Решение:** Увеличьте задержку с 0.05 до 0.1 секунды:
```python
await asyncio.sleep(0.1)
```

### Проблема 5: Реферальная ссылка не работает

**Решение:** Убедитесь, что получили username бота (см. Шаг 4)

---

## 📊 Итоговая структура проекта

```
telegram_bot/
├── main_UPGRADED.py ⭐ NEW VERSION
├── config.py
├── database.py ⭐ UPDATED
├── translations.py ⭐ UPDATED
├── middleware.py
├── requirements.txt
├── .env
│
├── handlers/
│   ├── __init__.py ⭐ UPDATED
│   ├── start_UPGRADED.py ⭐ NEW VERSION
│   ├── profile_UPGRADED.py ⭐ NEW VERSION
│   ├── admin_UPGRADED.py ⭐ NEW VERSION
│   ├── reviews.py ⭐ NEW FILE
│   ├── services.py
│   ├── about.py
│   ├── help.py
│   ├── order.py
│   └── subscription.py
│
├── keyboards/
│   ├── reply.py ⭐ UPDATED
│   └── inline.py ⭐ UPDATED
│
└── states/
    └── order.py ⭐ UPDATED
```

---

## 🎯 Чек-лист перед запуском

- [ ] База данных обновлена (referrer_id, balance добавлены)
- [ ] Все файлы с ⭐ заменены/обновлены
- [ ] handlers/__init__.py содержит reviews_router
- [ ] Получен username бота для реферальных ссылок
- [ ] Протестирована Web App кнопка
- [ ] Протестирована система отзывов
- [ ] Протестирована рассылка (от админа)
- [ ] Протестированы новые пакеты услуг
- [ ] Реферальная система работает корректно

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте логи бота: `logging.basicConfig(level=logging.DEBUG)`
2. Проверьте БД: `sqlite3 bot_database.db .schema`
3. Проверьте, что все роутеры зарегистрированы: `len(routers)` должно быть 9

---

**Версия:** 2.0.0 UPGRADED  
**Дата:** 30 декабря 2025  
**Статус:** ✅ Production Ready  
**Новые фичи:** Web App, Referral System, Reviews, New Packages, Enhanced Admin Panel

🚀 **Готово к деплою на PythonAnywhere!**
